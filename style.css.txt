/* =========================================
   TIGER WEBSITE — CSS
   Color Theme: Orange • Black • Cream • Brown
   ========================================= */

:root {
    --tiger-orange: #f28c28;
    --bright-orange: #ffad3d;
    --dark-orange: #c65f16;
    --tiger-black: #111111;
    --dark-brown: #1c120d;
    --brown: #3a2114;
    --cream: #f5ead7;
    --light-cream: #fff8ec;
    --gray: #c9c0b5;
}

/* =========================================
   RESET
   ========================================= */

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    font-family: Arial, Helvetica, sans-serif;
    background: var(--dark-brown);
    color: var(--cream);
    line-height: 1.7;
}

/* =========================================
   NAVIGATION
   ========================================= */

nav {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    padding: 18px 8%;

    display: flex;
    justify-content: space-between;
    align-items: center;

    background: rgba(17, 17, 17, 0.95);
    border-bottom: 3px solid var(--tiger-orange);

    z-index: 1000;
}

.logo {
    color: var(--tiger-orange);
    font-size: 1.5rem;
    font-weight: 900;
    letter-spacing: 2px;
}

nav ul {
    display: flex;
    list-style: none;
    gap: 30px;
}

nav a {
    color: var(--cream);
    text-decoration: none;
    font-weight: bold;
    transition: 0.3s ease;
}

nav a:hover {
    color: var(--tiger-orange);
}

/* =========================================
   TIGER STRIPE EFFECT
   ========================================= */

.stripes {
    background:
        repeating-linear-gradient(
            110deg,
            var(--tiger-black) 0px,
            var(--tiger-black) 12px,
            transparent 12px,
            transparent 42px
        );
}

/* =========================================
   HERO SECTION
   ========================================= */

.hero {
    min-height: 100vh;

    display: flex;
    justify-content: center;
    align-items: center;

    text-align: center;
    padding: 120px 20px 70px;

    background:
        linear-gradient(
            rgba(17, 17, 17, 0.55),
            rgba(28, 18, 13, 0.95)
        ),
        url("tiger.jpg") center / cover no-repeat;

    position: relative;
}

.hero::after {
    content: "";
    position: absolute;
    inset: 0;

    background:
        repeating-linear-gradient(
            115deg,
            transparent 0px,
            transparent 70px,
            rgba(0, 0, 0, 0.18) 70px,
            rgba(0, 0, 0, 0.18) 82px
        );

    pointer-events: none;
}

.hero-content {
    position: relative;
    z-index: 2;
    max-width: 900px;
}

.hero h1 {
    font-size: clamp(4rem, 10vw, 8rem);
    text-transform: uppercase;
    letter-spacing: 8px;

    color: var(--tiger-orange);

    text-shadow:
        5px 5px 0 var(--tiger-black),
        8px 8px 20px rgba(0, 0, 0, 0.8);
}

.hero p {
    max-width: 750px;
    margin: 20px auto;

    font-size: 1.4rem;
    color: var(--light-cream);
}

/* =========================================
   BUTTON
   ========================================= */

.hero-button {
    display: inline-block;

    margin-top: 25px;
    padding: 14px 35px;

    background: var(--tiger-orange);
    color: var(--tiger-black);

    border: 3px solid var(--tiger-black);
    border-radius: 5px;

    text-decoration: none;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 1px;

    transition: 0.3s ease;
}

.hero-button:hover {
    background: var(--bright-orange);
    transform: translateY(-4px);

    box-shadow:
        0 8px 20px rgba(0, 0, 0, 0.5);
}

/* =========================================
   GENERAL SECTIONS
   ========================================= */

section {
    padding: 100px 10%;
}

.section-title {
    position: relative;

    text-align: center;

    margin-bottom: 50px;

    font-size: 2.8rem;
    color: var(--tiger-orange);

    text-transform: uppercase;
    letter-spacing: 2px;
}

.section-title::after {
    content: "";

    display: block;

    width: 80px;
    height: 5px;

    margin: 15px auto 0;

    background: var(--tiger-orange);
}

/* =========================================
   INTRODUCTION
   ========================================= */

.intro {
    max-width: 900px;
    margin: auto;

    text-align: center;
    font-size: 1.15rem;
}

.intro p {
    margin-bottom: 20px;
}

/* =========================================
   PERSONAL STORY
   ========================================= */

.story {
    background:
        linear-gradient(
            135deg,
            var(--brown),
            var(--dark-brown)
        );

    border-top: 2px solid var(--dark-orange);
    border-bottom: 2px solid var(--dark-orange);
}

.story-container {
    max-width: 950px;
    margin: auto;
}

.story-container p {
    margin-bottom: 25px;
    font-size: 1.08rem;
}

/* Highlight important words */

.highlight {
    color: var(--tiger-orange);
    font-weight: bold;
}

/* =========================================
   TIGER QUOTE
   ========================================= */

.quote {
    padding: 110px 10%;

    text-align: center;

    background:
        linear-gradient(
            45deg,
            var(--tiger-black),
            var(--brown)
        );
}

.quote blockquote {
    max-width: 900px;
    margin: auto;

    font-size: clamp(1.7rem, 4vw, 2.8rem);

    font-style: italic;
    font-weight: bold;

    color: var(--light-cream);

    border-left: 6px solid var(--tiger-orange);
    padding-left: 25px;
}

.quote span {
    display: block;

    margin-top: 25px;

    color: var(--tiger-orange);
    font-weight: bold;
}

/* =========================================
   QUALITY CARDS
   ========================================= */

.cards {
    display: grid;

    grid-template-columns:
        repeat(3, 1fr);

    gap: 30px;

    max-width: 1200px;
    margin: auto;
}

.card {
    position: relative;

    padding: 35px;

    background: var(--brown);

    border: 1px solid #68402a;
    border-top: 5px solid var(--tiger-orange);

    border-radius: 8px;

    transition: 0.35s ease;

    overflow: hidden;
}

.card::before {
    content: "";

    position: absolute;

    top: 0;
    right: -40px;

    width: 120px;
    height: 300px;

    background: var(--tiger-black);

    transform: rotate(25deg);

    opacity: 0.15;
}

.card:hover {
    transform: translateY(-10px);

    border-color: var(--tiger-orange);

    box-shadow:
        0 15px 30px rgba(0, 0, 0, 0.45);
}

.card h3 {
    position: relative;

    color: var(--tiger-orange);

    font-size: 1.5rem;

    margin-bottom: 15px;
}

.card p {
    position: relative;

    color: var(--cream);
}

/* =========================================
   SYMBOLISM / FINAL SECTION
   ========================================= */

.symbolism {
    max-width: 950px;
    margin: auto;

    text-align: center;
}

.symbolism p {
    font-size: 1.15rem;
    margin-bottom: 20px;
}

/* =========================================
   TIGER STRIPE DIVIDER
   ========================================= */

.stripe-divider {
    height: 35px;

    background:
        repeating-linear-gradient(
            120deg,
            var(--tiger-black) 0px,
            var(--tiger-black) 20px,
            var(--tiger-orange) 20px,
            var(--tiger-orange) 60px
        );
}

/* =========================================
   FOOTER
   ========================================= */

footer {
    padding: 45px 20px;

    text-align: center;

    background: var(--tiger-black);

    border-top: 4px solid var(--tiger-orange);
}

footer p {
    color: var(--gray);
    margin: 5px;
}

footer p:first-child {
    color: var(--tiger-orange);
    font-size: 1.2rem;
    font-weight: bold;
}

/* =========================================
   RESPONSIVE DESIGN
   ========================================= */

@media (max-width: 900px) {

    .cards {
        grid-template-columns: repeat(2, 1fr);
    }

    nav {
        padding: 15px 5%;
    }

    nav ul {
        gap: 15px;
    }
}

@media (max-width: 650px) {

    nav {
        flex-direction: column;
        gap: 10px;
    }

    nav ul {
        flex-wrap: wrap;
        justify-content: center;
    }

    .hero h1 {
        font-size: 4rem;
    }

    .hero p {
        font-size: 1.1rem;
    }

    section {
        padding: 70px 7%;
    }

    .section-title {
        font-size: 2.1rem;
    }

    .cards {
        grid-template-columns: 1fr;
    }

    .quote {
        padding: 80px 7%;
    }
}

@media (max-width: 400px) {

    .hero h1 {
        font-size: 3rem;
        letter-spacing: 4px;
    }

    nav a {
        font-size: 0.8rem;
    }
}